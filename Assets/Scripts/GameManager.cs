using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameManager : MonoBehaviour
{
    //We want to be using this so store some info

    /*
        this will be repsonmsible for telling the gameplay viewer we need stuff in the scene. 
        Kinda a dispatch monkey that will just send "requests" to different parts of the script.
    */
    
}
